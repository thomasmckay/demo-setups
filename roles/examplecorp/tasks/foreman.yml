---
- name: "{{ name }}"
  local_action:
    module: "{{ entity_module[entity] }}"
    username: "{{ username }}"
    password: "{{ password }}"
    server_url: "{{ server }}"
    entity: "{{ entity }}"
    params: "{{ params }}"
